//1. **String Creation**:
const firstName = 'Rohini'; //Create a string variable using single quotes.
const lastName = "Mittal";  //Create another string variable using double quotes.
const message = `Hii! ${firstName} ${lastName}`;

//2. **String Concatenation**:

const finalString = firstName + " " + lastName + " " + message;
console.log(finalString);
